export default function Simple_Component() {
  return (
    <div>
      <h1>This is a basic component</h1>
      <p>
        Components can only return one element, you can put them inside one
        element to go around this rule.
      </p>
    </div>
  );
}
